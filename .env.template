# .env.template
# =====================================================================================
# Sample configuration file for the CryptoVol.ai project
# ====================================================================================================================
#
# Instructions:
# 1. Copy this file to a new file named `.env` in the same directory.
# 2. Fill in the variables below with your real or test values.
# 3. Make sure the `.env` file is added to `.gitignore` and is not pushed to the repository.

# --- Docker port mapping settings ---
# These variables define on which ports of your computer (HOST) the services running inside Docker containers will be available.
# You can change them if some ports are already taken.

# --- PostgreSQL database settings ---
# These variables are used to initialize the database in the Docker container,
# and also to connect to it from your FastAPI application.

POSTGRES_USER=myuser
POSTGRES_PASSWORD=supersecretpassword
POSTGRES_DB=cryptovol_db

# IMPORTANT: When running via docker-compose, the host for the FastAPI application
# must be the name of the database service from docker-compose.yml.
POSTGRES_HOST=db
POSTGRES_PORT=5432

# --- JWT and security settings for FastAPI ---
# These variables are used by your backend application to create and validate
# authentication tokens.

# Generate a complex and unique key.
# You can use the command in the terminal: openssl rand -hex 32
SECRET_KEY=your_super_secret_and_long_random_string_generated_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Port where your API will be available
APP_PORT=8000
# Port for connecting to the PostgreSQL database from your computer (e.g. via DBeaver)
DB_PORT=5432
# Port for the pgAdmin web interface
PGADMIN_PORT=5050
# Port for connecting to Redis
REDIS_PORT=6379

# --- pgAdmin web interface settings ---
# Credentials for logging into pgAdmin.

PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
