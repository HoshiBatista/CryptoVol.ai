# .env.template
# ==============================================================================
#                 Пример файла конфигурации для проекта CryptoVol.ai
# ==============================================================================
#
# Инструкция:
# 1. Скопируйте этот файл в новый файл с именем `.env` в этой же директории.
# 2. Заполните переменные ниже вашими реальными или тестовыми значениями.
# 3. Убедитесь, что файл `.env` добавлен в `.gitignore` и не будет загружен в репозиторий.

# --- Настройки маппинга портов для Docker ---
# Эти переменные определяют, на каких портах вашего компьютера (HOST) будут доступны
# сервисы, работающие внутри Docker-контейнеров.
# Вы можете изменить их, если какой-то порт уже занят.

# --- Настройки базы данных PostgreSQL ---
# Эти переменные используются для инициализации базы данных в Docker-контейнере,
# а также для подключения к ней из вашего FastAPI-приложения.

POSTGRES_USER=myuser
POSTGRES_PASSWORD=supersecretpassword
POSTGRES_DB=cryptovol_db


# ВАЖНО: При запуске через docker-compose, хостом для FastAPI-приложения
# должно быть имя сервиса базы данных из docker-compose.yml.
POSTGRES_HOST=db
POSTGRES_PORT=5432


# --- Настройки JWT и безопасности для FastAPI ---
# Эти переменные используются вашим бэкенд-приложением для создания и проверки
# токенов аутентификации.

# Сгенерируйте сложный и уникальный ключ.
# Можно использовать команду в терминале: openssl rand -hex 32
SECRET_KEY=your_super_secret_and_long_random_string_generated_here
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30


# Порт, на котором будет доступен ваш API
APP_PORT=8000
# Порт для подключения к базе данных PostgreSQL с вашего компьютера (например, через DBeaver)
DB_PORT=5432
# Порт для веб-интерфейса pgAdmin
PGADMIN_PORT=5050
# Порт для подключения к Redis
REDIS_PORT=6379


# --- Настройки веб-интерфейса pgAdmin ---
# Учетные данные для входа в pgAdmin.

PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=admin
